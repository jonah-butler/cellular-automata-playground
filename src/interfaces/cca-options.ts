export default interface CCAOptions {
  colors: string[];
  range: number;
  threshold: number;
  generations: number;
  width: number;
  cellSize: number;
  lifeCycles: number;
  useNoise: boolean;
  animate: boolean;
}